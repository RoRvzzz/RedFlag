#pragma once

// Placeholder for class images data
// In a real implementation, this would contain the actual image data
extern const unsigned char ClassImagesRawData[];
extern const size_t ClassImagesRawDataSize;

// For now, we'll create a simple placeholder
const unsigned char ClassImagesRawData[] = {
    // This is a placeholder - in reality this would be binary image data
    0x00, 0x01, 0x02, 0x03
};

const size_t ClassImagesRawDataSize = sizeof(ClassImagesRawData);
